# Stage 17 多市场原始数据采集验收记录

## Stage 17.7 最新正式验收结论（2026-08-26）

- 业务基准日：`2026-08-24`。
- 最新正式批次：`4a5599fb-9e60-4013-9c0c-69fcc25a98b2`。
- 阶段出口状态：`PASS`。
- Stage 18入口授权：`true`；本任务没有启动Stage 18，也没有创建Stage 18或Stage 19产物。
- 本结论只以该完整批次自身证据为依据，没有拼接任何专项或中断批次。

### 正式出口结果

- A股日线：16只 × raw/qfq/hfq，`48/48 PASS`。
- 港股日线：7只 × raw/qfq/hfq，`21/21 PASS`。
- A/H股日线合计：`69/69`非空且`69/69`通过质量和严格上市覆盖门禁。
- OKX `ETH-USDT`：`1m、3m、5m、15m、1h、1d`共`6/6 PASS`。
- 证券档案与上市日期证据：`23/23 PASS`。
- 分钟可行性：`0/10`；首次连接类失败后其余探针以明确的skipped失败闭集。该项只作可行性披露，不属于Stage 17正式日线/ETH出口集合。
- Raw闭集：`PASS`；Manifest登记196个文件，重新计算SHA-256后不一致数为0，残留`.tmp`为0。
- 旧正式批次`78cddc46-4b2a-4ebe-9065-1e54e3dff522`的198个Raw文件重新计算SHA-256后不一致数仍为0。

### 港股正式来源选择

| 标的 | raw | qfq | hfq |
|---|---|---|---|
| 02076.HK | 腾讯 | 腾讯 | 腾讯 |
| 02180.HK | 腾讯 | 腾讯 | 腾讯 |
| 06100.HK | 腾讯 | 腾讯 | 腾讯 |
| 06919.HK | 腾讯 | 腾讯 | 腾讯 |
| 08365.HK | 腾讯 | 腾讯 | 腾讯 |
| 08462.HK | 腾讯 | 腾讯兼容直连 | 腾讯兼容直连 |
| 09669.HK | 腾讯 | 腾讯兼容直连 | 腾讯兼容直连 |

Registry不是按“先返回即选用”，而是每个候选依次通过身份、OHLC、日期、上市覆盖和复权语义检查后才正式选用。`08462.HK`和`09669.HK`的兼容直连均记录调整端点请求以及`qfqday/hfqday`缺失时读取`day`字段的回退事实。

`08462.HK`与`09669.HK`在本批次中各自的qfq与hfq序列完全相同。该事实不按已批准口径自动判为失败，但只代表Provider本次原生返回结果相同，不得解释为已经证明标的不存在复权事件；09669的Manifest字段`qfq_hfq_identical=true`且`qfq_hfq_identical_is_failure=false`。

### 历史批次处置

- `68dcbbe5-e065-4737-8688-f909d9f74dc9`为闭合集合但只有67/69日线通过的`BLOCKED`正式证据，失败项为`08462.HK qfq/hfq`；该结论保留，不覆盖。
- `c5b6dcc0-b02e-4b4a-94c3-53d8276c0644`、`f5c791f4-ff36-43ec-8c25-8642d74c7440`、`51f27f3d-2e2b-4cfe-abe9-37fb2b989768`和`103ea6f8-8cc3-4ce0-a54b-070b351cdd3a`为中断批次，仅保留不可变Raw证据，不参与验收或补洞。
- 以下历史`BLOCKED`章节继续保留用于阶段沿革，但已被本节最新完整PASS批次取代，不再代表当前Stage 17状态。

## 历史重新审计结论（已由 Stage 17.7 PASS 批次取代）

- 验收日期与业务基准日：`2026-08-24`。
- 最新正式批次：`78cddc46-4b2a-4ebe-9065-1e54e3dff522`。
- 阶段出口状态：`BLOCKED`。
- Stage 18授权：`false`。
- 本次没有建立Stage 17条件封板。

重新审计修复了数据源韧性、来源审计和统计语义，但没有放宽质量门禁。最新结果仍不能
标记为`PASS`，项目必须停在Stage 17。

### 修复后正式结果

- 正式日线逻辑集合仍为23只证券 × 3种复权，共69项。
- A股48/48全部取得非空Raw并通过质量和上市覆盖检查：8项选用东方财富主源，40项在
  主源连接失败后显式选用新浪备用源。
- 港股21/21均未形成正式可用日线：东方财富主源21项均为`connection_error`；新浪
  21项候选均取得非空Raw，但全部存在`ohlc_logic_error`，类别为
  `upstream_ohlc_inconsistency`，因此没有被选为正式数据。
- 正式日线非空数为48/69，质量通过数也是48/69；兼容字段`daily_success`明确只等于
  `daily_nonempty_count`，不得解释为质量通过数。
- OKX `ETH-USDT`六个周期全部成功并通过质量检查。
- 分钟可行性为1/10成功；其余失败如实进入清单。分钟样本不属于69项日线和6项ETH的
  正式出口集合，但限制必须披露。
- Raw清单闭集为`PASS`：清单登记198个文件，逐文件大小和SHA-256齐全，残留临时文件0。
- 48项正式A股日线均为`complete_to_verified_listing_date`；21项港股因无合格来源保持
  `history_unavailable`，未冒充上市以来完整覆盖。

### 运行中断记录

批次`36a2d9f7-4696-451e-a91b-8f314809ea85`在旧分钟接口长等待时被主动中止，保留
184个已落盘Raw文件作为不完整运行证据。该批次没有形成正式报告，不得用于验收、拼接
或补齐最新正式批次。中断后已加入适配器层默认HTTP超时，并使用新的正式`run_id`重跑。

### 最新门禁

新浪备用源解决了A股连接失败，但没有解决港股OHLC真实性问题。Stage 17后续若要达到
`PASS`，仍需在新的完整批次中取得21项合法港股日线，或由可审计来源解释并替换异常
来源；禁止修改、删除或插值修复上游OHLC。满足前Stage 18继续禁止进入。

## 修复前验收结论（历史）

- 验收日期：`2026-08-24`。
- 业务基准日：`2026-08-24`。
- 修复前正式批次：`7c84e2f1-5a39-4e62-9d17-172026082424`。
- 实施状态：采集、存储、质量检查、报告和CLI均已建立并完成正式运行。
- 阶段出口状态：`BLOCKED`。
- Stage 18授权：`false`。

Stage 17不能标记为`PASS`，不能依据本次任务进入Stage 18，也未建立Stage 17条件封板。

## 正式结果

### A/H股日线

- 正式期望：23只证券 × 3种复权口径，共69个数据集。
- 成功取得非空Raw：49/69。
- 其中通过质量检查：43个。
- 连接失败：20个，其中A股11个、港股9个；失败状态与空数据分开记录。
- 港股另有6个非空数据集存在真实`ohlc_logic_error`，未被降级为通过。
- 所有成功数据集均保留未复权/前复权/后复权身份及实际日期范围。

阻塞原因以
`reports/stage17/7c84e2f1-5a39-4e62-9d17-172026082424/stage17_manifest.json`
中的26项`blocked_risks`为准。主要阻塞是东方财富历史接口连接失败及港股上游OHLC不一致。

### 上市日期和严格历史覆盖

- A股上市日期使用AKShare巨潮公司概况中的明确“上市日期”字段。
- 港股证券档案Raw已核对，7只港股均包含明确“上市日期”列，不使用描述文本推断。
- 成功且覆盖到已核验上市区间：A股37个日线数据集、港股12个日线数据集。
- 因对应历史请求失败而无法完成覆盖判断：9只证券所涉及的数据集，保持`history_unavailable`。
- 未把数据源最早日期冒充上市日期，也未把未知状态改写为完整覆盖。

### ETHUSDT

- 数据身份：OKX现货`ETH-USDT`，项目规范代码`ETHUSDT`，UTC，仅保存确认K线。
- `1m、3m、5m、15m、1h、1d`六个周期全部成功并通过身份、连续性、OHLC和成交量检查。
- 90天周期实际起点均为`2026-05-27T00:00:00Z`。
- `1h/1d`数据源实际最早可得时间分别为`2018-01-11T11:00:00Z`和
  `2018-01-11T00:00:00Z`；未宣称早于该时间可得。

### 分钟可行性

- A/H股10个直接分钟样本全部成功：5个标的 × `1m/5m`。
- 5个`3m`结果只在报告层从`1m`演示聚合，状态为`report_only_derived`。
- 未写入`3m`衍生Raw，未宣称AKShare直接支持A/H股3分钟行情。

## 完整性检查

- Raw清单闭集：`PASS`。
- 当前批次清单登记Raw文件：176个，均包含路径、大小和SHA-256。
- 残留`.tmp`文件：0。
- 旧Raw未覆盖；三个正式运行批次使用不同`run_id`。
- Stage 0三个冻结文件SHA-256保持不变。
- 未创建或修改Stage 18配置、财务数据、数据库表或Stage 19事件结果。

## 运行批次沿革

1. `9a0c3c18-5b7d-4d58-a719-172026082424`：首次正式运行，40/69日线成功；暴露上市日期列解析和长期复权负价误报问题。
2. `3f2b6d7a-47f1-4f80-b317-172026082424`：修复后重跑，45/69日线成功；继续收紧行式上市日期标签。
3. `7c84e2f1-5a39-4e62-9d17-172026082424`：修复前正式结论，49/69日线成功、6/6 ETH成功，状态`BLOCKED`。

前三批Raw和报告按不可变原则保留，仅作为修复和重试审计证据；不得替代或拼接成最新正式批次。

## 下一步门禁

Stage 17后续若要达到`PASS`，至少需要：

1. 在新`run_id`下重新完成69个A/H股日线数据集，不能用不同批次拼接补洞；
2. 解决或由权威来源解释港股OHLC不一致，不能静默修正Raw；
3. 所有日线、六个ETH周期、上市覆盖和清单检查同时通过；
4. 另行完成Stage 17正式验收。

在上述条件满足前，阶段衔接规则禁止进入Stage 18。

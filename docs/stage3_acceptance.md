# 阶段3独立验收报告

> 验收日期：2026-07-29
> 正式运行 ID：`39a6996e-36d7-4b73-b0b8-c38da4ae672e`
> 验收结论：**PASS**
> 是否允许进入阶段4：**是**

## 1. 验收边界

本次验收完全离线，没有调用 AKShare 或其他真实数据接口，没有修改业务代码、
测试、阶段0配置或正式 Raw，也没有重新抓取数据。仅生成独立验收报告和统计表。

## 2. 前置门禁

| 检查 | 结果 |
|---|---|
| `pip check` | PASS，退出码0 |
| 完整 pytest | 121 passed，退出码0 |
| doctor | PASS，Python 3.11.4，14/14 包可导入 |
| 阶段0三个SHA-256 | 与冻结基准完全一致 |
| 真实网络访问 | 无 |

## 3. Coverage与Raw文件

- Coverage CSV 可解析，共49条，run_id全部一致。
- 16条qfq、16条raw、1条全市场spot、16条目标股票spot。
- 32条日线全部success；49条记录均为PASS，无错误或警告计数。
- Manifest登记34个Raw：32个历史日线Parquet和2个spot Parquet。
- 34/34文件存在、非空、可读取、位于正确run_id分区。
- 34/34文件大小及SHA-256与manifest一致。
- 所有manifest路径均为项目相对路径，未发现个人绝对路径或敏感值。
- Raw仍保留AKShare原始中文字段，未发现清洗、翻译或单位转换。

## 4. 历史日线质量

32个文件全部满足：

- 非空且包含日期、开盘、收盘、最高、最低、成交量、成交额；
- 最早日期为2023-07-27，最晚日期为2026-07-27；
- 日期可解析、升序、无重复，且未晚于业务日期；
- OHLC关系无错误；
- 成交量和成交额无负数；
- 每只股票qfq与raw日期集合、行数完全一致。

除600438和601500外，其余14只股票每种口径均为726行。600438为716行，
601500为722行；两者的qfq与raw行数仍完全一致。

## 5. 较少行数股票

采用16只股票日期集合并集作为比较基准：

- `600438` 缺少2026-02-25至2026-03-10的10个连续交易日期。
- `601500` 缺少2025-03-27至2025-04-01的4个连续交易日期。
- 两只股票的缺失日期在qfq和raw中完全一致。
- 每只股票仅形成一个连续缺失区段，未发现随机散落断层或复权口径不一致。

在不联网查询公告的边界下，这两组缺失符合停牌样特征，判为 **WARN**，
不构成阶段3失败。逐日证据见
`reports/stage3_acceptance_missing_dates.csv`。

## 6. Spot完整度

- 快照时间：`2026-07-29T11:35:34.785065+08:00`，未伪装为业务日期。
- 全市场Raw为5884行；目标子集为16个唯一六位代码，无缺失。
- 完整市场Raw和目标子集均存在，列集合完全一致。
- 代码、名称、最新价、成交额、振幅、量比、换手率、动态市盈率、市净率、
  总市值和流通市值均存在。
- 11个字段在16只目标股票中均为16/16非空；10个代码/数值字段均为16/16可解析。
- 未填补或伪造估值字段。

完整统计见 `reports/stage3_acceptance_spot_completeness.csv`。

## 7. 追加式和原子存储

静态审查与临时目录测试确认：

- 路径包含run_id、adjust和symbol；
- 已存在最终文件时抛出`FileExistsError`，不静默覆盖；
- 先写`.tmp`，成功后使用`os.replace`原子替换；
- 写入失败时清理临时文件；
- Raw写入过程中没有字段翻译、清洗、去重或单位转换。

相关Raw存储及阶段边界子集共41项测试通过。

## 8. 测试变更审查

`tests/test_stage1_structure.py` 当前仍保留：

- 阶段0哈希断言；
- Clean、Feature、Export目录不得生成数据的断言；
- Raw顶层仅允许阶段3两个接口目录的白名单；
- 业务DDL关键字检查；
- AKShare/HTTP调用文本扫描；
- 无无条件skip或xfail。

修改只放开了可追溯的阶段3Raw目录，未把Clean或Feature排除检查，也未弱化
阶段0门禁。证据限制：该测试文件在当前Git基线中未被跟踪，无法取得历史
Git diff；本结论基于当前断言和阶段1文档的对照审查。

## 9. 阶段边界

未发现阶段4批量财务或资金流采集器。源码中的财务接口名称仅存在于阶段2
小样本探针。未发现Clean、Feature、Export数据、业务数据库、均线、活跃度、
涨跌停事件、基本面或横盘风格实现。

## 10. 报告一致性

实际文件与stage3文档、coverage、run JSON、spot字段报告和manifest中的以下
数字一致：32个日线任务、16个qfq成功、16个raw成功、34个Raw文件、49条
coverage、全市场5884行、目标股票16/16。

`docs/stage3_market_fetch.md`没有写入pytest数量，但本次独立离线验收确认
121项测试全部通过；这是文档完整性警告，不是数据或质量失败。

## 11. 失败与警告

失败：无。

警告：

1. 600438存在一个10交易日的停牌样缺失区段，qfq/raw一致。
2. 601500存在一个4交易日的停牌样缺失区段，qfq/raw一致。
3. `tests/test_stage1_structure.py`未被Git跟踪，无法取得历史diff。
4. 阶段3抓取文档未嵌入pytest数量；独立验收实测为121 passed。

## 12. 结论

阶段3的Raw、覆盖、日线质量、spot完整度、追加式存储、测试门禁和阶段边界
均满足验收条件。上述警告不影响数据一致性或阶段边界。

> 阶段3已完成独立验收，可以进入阶段4：财务与资金流抓取。

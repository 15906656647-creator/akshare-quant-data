# AKShare 数据抓取测试项目总纲

> 文档日期：2026-07-27  
> 项目性质：数据能力验证与量化研究原型  
> 参考项目：[AKShare GitHub](https://github.com/akfamily/akshare) / [AKShare 在线文档](https://akshare.akfamily.xyz/)

## 1. 项目背景

本项目使用 AKShare 搭建一套可复现的量化金融数据测试流程，验证从公开数据源获取 A 股及加密货币相关数据的可行性，并跑通以下数据链路：

**数据获取 → 原始数据留存 → 标准化清洗 → 数据库存储 → 指标计算 → 条件筛选 → 统计分析 → 结果展示 → 质量监控。**

AKShare 当前定位为基于 Python 的开源财经数据接口库，覆盖股票、期货、基金、外汇、债券、指数、加密货币等数据。其接口依赖外部公开网页和数据源，因此本项目不仅验证“能否获取”，还要验证接口稳定性、字段一致性、历史覆盖范围及数据可追溯性。

## 2. 项目目标

### 2.1 核心目标

1. 完成 AKShare 本地运行环境搭建，形成可重复安装的依赖文件。
2. 对指定 16 只 A 股和 ETHUSDT 进行数据抓取测试。
3. 验证股票行情、财务指标、财务报表、估值、资金流、涨跌停等数据的可获得性。
4. 建立原始层、清洗层、指标层和分析层的数据结构。
5. 使用数据库保存数据，并提供 SQL 查询及数据展示样例。
6. 计算均线、量价活跃度、涨跌停统计、次日表现、横盘震荡及疑似主力行为特征。
7. 输出完整的 AKShare 数据搭建报告，为后续定时任务、看板或策略研究提供基础。

### 2.2 非目标

本阶段不执行自动交易，不将“主力行为”模型作为确定事实，不承诺 AKShare 所有接口长期稳定，也不将分析结果作为投资建议。

## 3. 测试样本

### 3.1 A 股样本

| 序号 | 股票代码 | 市场前缀规则 |
|---:|---|---|
| 1 | 002067 | SZ |
| 2 | 002600 | SZ |
| 3 | 002230 | SZ |
| 4 | 600763 | SH |
| 5 | 603259 | SH |
| 6 | 603799 | SH |
| 7 | 601012 | SH |
| 8 | 600438 | SH |
| 9 | 002361 | SZ |
| 10 | 601500 | SH |
| 11 | 600231 | SH |
| 12 | 300274 | SZ |
| 13 | 601636 | SH |
| 14 | 002129 | SZ |
| 15 | 000100 | SZ |
| 16 | 300433 | SZ |

市场前缀在不同接口中可能有不同格式：

- `stock_zh_a_hist` 通常使用纯六位代码，例如 `600763`。
- 东方财富财务报表接口通常使用 `SH600763` 或 `SZ002067`。
- 资金流接口通常拆分为 `stock="600763"` 与 `market="sh"`。

项目必须建立统一的证券主数据表，集中维护 `symbol`、`exchange`、`symbol_em`、`symbol_lower` 等映射，不允许在业务代码中散落拼接规则。

### 3.2 加密货币样本

- `ETHUSDT`

AKShare 文档明确提供主流加密货币实时行情接口，例如 `crypto_js_spot`，但其返回品种及市场由数据源决定，不等同于保证存在 Binance 的 `ETHUSDT` 历史 K 线。测试阶段应分两步：

1. 验证 AKShare 加密货币实时行情是否包含 ETH 或 ETHUSD/ETHUSDT 类品种。
2. 若必须获得标准化 `ETHUSDT` 历史 OHLCV，应将其列为“AKShare 能力缺口验证项”；需要时引入独立适配器调用交易所公开接口或 CCXT，并在报告中明确该数据不是由 AKShare 提供。

## 4. 数据可获取性初步结论

### 4.1 能直接通过 AKShare 获取的数据

| 数据主题 | 建议接口 | 主要字段或用途 | 初步可行性 |
|---|---|---|---|
| A 股历史日线 | `stock_zh_a_hist` | 日期、开高低收、成交量、成交额、振幅、涨跌幅、换手率 | 高 |
| A 股实时行情与估值 | `stock_zh_a_spot_em` | 最新价、量比、换手率、动态市盈率、市净率、市值 | 高，但属于时点快照 |
| 个股关键财务指标 | `stock_financial_abstract` | 营业收入、净利润、归母净利润等历史指标 | 中高，字段需转置和标准化 |
| 财务分析指标 | `stock_financial_analysis_indicator` | 每股指标、盈利能力、成长能力、偿债能力等 | 中高 |
| 资产负债表 | `stock_balance_sheet_by_report_em` | 报告期资产、负债、所有者权益等 | 高，字段多且为英文编码 |
| 利润表 | `stock_profit_sheet_by_report_em` | 营业收入、营业成本、利润等 | 高，字段多且需映射 |
| 现金流量表 | `stock_cash_flow_sheet_by_report_em` | 经营、投资、筹资现金流 | 高，字段多且需映射 |
| 个股资金流 | `stock_individual_fund_flow` | 主力、超大单、大单、中单、小单净流入 | 中，仅近约 100 个交易日 |
| 近期涨停股池 | `stock_zt_pool_em` | 封板时间、封板资金、炸板次数、连板数 | 中，仅近期数据 |
| 近期跌停股池 | `stock_zt_pool_dtgc_em` | 跌停、连续跌停、开板次数等 | 中，仅近期数据 |
| 加密货币实时行情 | `crypto_js_spot` | 主流币种实时报价、24 小时高低及成交量 | 中，需核对是否包含目标交易对 |

### 4.2 需要自行计算或建模的数据

| 需求 | 数据基础 | 计算方式 |
|---|---|---|
| 3、5、7、10、13、20、21 日均线 | 日线收盘价 | `rolling(window).mean()` |
| 成交量均线与量比 | 日线成交量 | 成交量 / N 日均量，或滚动均量 |
| 股票活跃度 | 涨跌幅、振幅、换手率、成交额、量比 | 标准化后加权评分或分位数评分 |
| 经常放量 | 成交量、成交额 | 当日量相对 20/60 日均量倍数及出现频率 |
| 经常大涨大跌 | 日收益率、振幅 | 超过阈值的天数、尾部波动、ATR、实现波动率 |
| 一年涨停/跌停次数 | 不复权日线、证券状态、涨跌停规则 | 按交易日和证券状态动态识别 |
| 涨停后次日开盘表现 | 涨停日与次日开盘价 | 次日开盘缺口、最高收益、收盘收益、是否连板 |
| 横盘震荡 | 收盘、最高、最低、成交量 | 箱体宽度、趋势斜率、波动率、布林带宽度、突破次数 |
| 暴力或温和洗盘风格 | 振幅、回撤、放量、资金流、价格恢复 | 特征组合评分或聚类，不应直接当作事实标签 |
| 主力资金行为 | 资金流、量价、龙虎榜/大宗交易等 | 多源证据评分，只能表述为“疑似”或“特征” |
| 盈亏比 | 需先定义口径 | 若指策略盈亏比，必须基于明确买卖信号；若指上涨/下跌收益比，可从收益分布计算 |

## 5. 总体技术架构

```text
外部公开数据源
      ↓
AKShare 接口适配层
      ↓
抓取任务层（重试、限速、日志、字段快照）
      ↓
Raw 原始层（Parquet/JSON，可追溯、不覆盖）
      ↓
Clean 清洗层（统一字段、类型、单位、主键）
      ↓
Database 数据库层（DuckDB/SQLite 测试；PostgreSQL 生产）
      ↓
Feature 指标层（均线、波动率、量价、涨跌停、箱体）
      ↓
Analysis 分析层（筛选、评分、事件研究、横截面对比）
      ↓
Report 报告与可视化层
```

### 5.1 推荐技术栈

- Python：64 位 Python 3.12；AKShare README 最低要求为 64 位 Python 3.9 及以上。
- 数据处理：`pandas`、`numpy`。
- 本地分析数据库：`DuckDB`，适合直接查询 Parquet；可同时提供 SQLite 样例。
- 生产数据库：PostgreSQL。
- 文件格式：原始及清洗数据优先 Parquet，CSV 仅用于人工查看或交付样例。
- 数据质量：`pandera` 或自定义断言。
- 调度：测试阶段使用命令行；后续可选 cron、APScheduler、Airflow 或 Prefect。
- 可视化：`matplotlib`、`plotly` 或 BI 工具。
- 配置管理：`.env`、YAML；日志使用 Python `logging`。

## 6. 数据分层设计

### 6.1 Raw 原始层

原则：保留 AKShare 返回字段、抓取时间、接口名、参数、AKShare 版本、成功状态和错误信息，不直接覆盖历史快照。

建议目录：

```text
data/raw/{interface_name}/trade_date=YYYY-MM-DD/*.parquet
```

### 6.2 Clean 清洗层

完成以下标准化：

- 股票代码统一为六位字符串，禁止转成整数。
- 交易所统一为 `SH`、`SZ`、`BJ`。
- 日期统一为 `date`，时间统一为带时区或明确时区的 `timestamp`。
- 价格统一为数值型，金额统一为人民币元，成交量同时保存原始“手”和转换后的“股”。
- 百分比字段统一明确是 `5.20` 还是 `0.052`，建议数据库保存小数值并在展示层转百分比。
- 复权行情与不复权行情分表或通过 `adjust_type` 区分。
- 财务报告期、公告日期、数据更新时间分开保存，禁止混用。
- 原始字段名与标准字段名建立数据字典。

### 6.3 Feature 指标层

重点指标：

- `ma_3`、`ma_5`、`ma_7`、`ma_10`、`ma_13`、`ma_20`、`ma_21`
- `volume_ma_5`、`volume_ma_20`、`volume_ratio_20`
- 日收益率、区间收益率、年化波动率、ATR、振幅均值
- 换手率均值和高换手天数
- 放量次数、大涨次数、大跌次数、跳空次数
- 涨停/跌停标记、连续涨停数、涨停次日收益
- 20/60 日箱体宽度、线性趋势斜率、布林带宽度
- 资金流净额和净占比滚动指标
- 活跃度评分、异常量价评分、横盘评分、洗盘风格评分

## 7. 数据库设计概要

建议最小表集：

| 表名 | 内容 | 建议主键 |
|---|---|---|
| `dim_security` | 证券代码、名称、市场、板块、上市状态 | `symbol` |
| `fact_stock_daily` | A 股日线行情 | `symbol, trade_date, adjust_type` |
| `fact_stock_spot` | 实时行情和估值快照 | `symbol, snapshot_time` |
| `fact_income_statement` | 利润表标准字段 | `symbol, report_date, report_type` |
| `fact_balance_sheet` | 资产负债表标准字段 | `symbol, report_date, report_type` |
| `fact_cashflow_statement` | 现金流量表标准字段 | `symbol, report_date, report_type` |
| `fact_financial_indicator` | 关键财务指标 | `symbol, report_date, metric_code` 或宽表主键 |
| `fact_fund_flow_daily` | 个股资金流 | `symbol, trade_date` |
| `fact_limit_event` | 涨停、跌停、炸板及事件后表现 | `symbol, trade_date, event_type` |
| `fact_crypto_quote` | 加密货币实时行情 | `market, pair, snapshot_time` |
| `feature_stock_daily` | 每日技术及量价特征 | `symbol, trade_date` |
| `analysis_stock_profile` | 股票股性与风格汇总 | `symbol, as_of_date, lookback_days` |
| `etl_run_log` | 抓取批次、耗时、行数、错误信息 | `run_id` |
| `data_quality_result` | 数据质量检查结果 | `run_id, check_name` |

## 8. 分析口径设计

### 8.1 均线

使用前复权收盘价计算趋势均线；使用不复权价格识别真实涨跌停。均线窗口为 `3、5、7、10、13、20、21` 个交易日。

### 8.2 活跃度

建议拆为四个子分数：

1. **流动性分数**：成交额、换手率、有效交易天数。
2. **波动分数**：收益率标准差、振幅、ATR。
3. **事件分数**：大涨、大跌、涨停、跌停、跳空次数。
4. **量价异常分数**：放量上涨、放量下跌、价量背离、异常资金流。

总分建议使用横截面分位数或 Z-score 加权，并保留各子分项，避免黑箱结果。

### 8.3 涨跌停事件

一年内涨跌停次数不能只用固定 `±10%` 判断。必须考虑：

- 股票所属板块和交易所。
- 是否 ST、*ST 或存在其他特别处理。
- 上市初期或特定交易日是否存在不同价格限制。
- 价格四舍五入和最小报价单位。
- 证券状态在历史期间是否发生变化。

优先使用官方或行情接口提供的当日涨停价、跌停价；若历史接口不直接给出，则维护“规则表 + 证券状态历史表”，并用实际收盘价与理论限价容差匹配。

次日表现至少计算：

- 次日开盘相对涨停收盘的缺口收益。
- 次日开盘相对涨停日前收盘的收益。
- 次日最高、最低、收盘收益。
- 次日是否继续涨停、是否高开低走、是否炸板。
- 未来 3、5、10 日累计收益和最大回撤。

### 8.4 横盘震荡与洗盘风格

“横盘震荡洗盘”不是可直接观测的客观字段，应拆为可测特征：

- 价格在 N 日区间内的高低点宽度。
- 线性回归斜率是否接近零。
- 布林带宽度是否收窄。
- 成交量是否逐步萎缩或间歇放大。
- 是否频繁出现长上下影线、冲高回落、快速收复。
- 突破箱体后是否伴随放量。
- 资金流是否与价格方向背离。

风格输出建议分为：`温和箱体型`、`高波动震荡型`、`放量冲击型`、`低活跃盘整型`、`趋势型`、`证据不足`，同时输出置信度和解释字段。

## 9. 数据清洗需求

1. 缺失值：区分停牌、未披露、接口异常和字段本身不适用。
2. 重复值：按主键去重，并保存抓取批次和最新更新时间。
3. 类型转换：代码保持字符串；日期、数值、百分比、金额分别规范。
4. 复权一致性：技术指标统一使用同一种复权口径；事件识别使用不复权。
5. 单位转换：成交量“手/股”、金额“元/万元/亿元”必须明确。
6. 财报累计值与单季度值：利润表和现金流量表的累计值不能直接当单季度值，需做差分并处理一季度。
7. 财务修订：同一报告期可能有后续修订，需保留公告日期、更新时间和版本。
8. 异常值：零价格、负成交量、极端涨跌幅、OHLC 逻辑错误需标记。
9. 交易日对齐：使用交易日历，不能用自然日简单补齐。
10. 数据源差异：AKShare 的不同接口可能来自不同网站，需记录 `source_name`。
11. 加密货币时间：明确 UTC 与北京时间，避免日界线错位。

## 10. 风险点

| 风险 | 影响 | 应对方式 |
|---|---|---|
| 上游网页变更导致接口失效 | 抓取中断 | 锁定版本、接口冒烟测试、备用接口、错误告警 |
| 接口被限速或封禁 | 批量任务失败 | 降低并发、随机等待、指数退避、缓存、分批抓取 |
| 历史涨跌停池仅支持近期 | 无法直接统计全年 | 以日线和历史规则自行重建事件，股池仅用于校验 |
| 实时估值是时点数据 | 无法天然回溯历史 PE/PB | 每日定时快照；需要历史估值时验证其他 AKShare 接口或补充数据源 |
| 财报字段多且频繁变化 | 清洗维护成本高 | 建立字段映射表、关键字段白名单、Schema 版本 |
| 前复权历史会随除权调整 | 回测重现差异 | 保存抓取日期、复权类型和原始数据版本 |
| 主力行为不可直接证实 | 结论被误读 | 使用“疑似/特征”表述，展示证据和置信度 |
| ETHUSDT 能力不明确 | 无法完成历史 K 线测试 | 先验证 AKShare；能力不足时启用独立交易所适配器并明确来源 |
| 股票名称或 ST 状态变化 | 涨跌停判断错误 | 保存证券状态历史，不使用当前名称反推历史状态 |
| 数据用途和授权风险 | 商业使用受限 | 遵守 AKShare 和上游数据源声明，仅用于合规研究与测试 |

## 11. 测试与验收标准

### 11.1 环境验收

- 能输出 Python、AKShare、pandas、DuckDB 版本。
- 16 只股票代码均能完成证券主数据映射。
- 核心接口的单股票测试至少成功一次。

### 11.2 数据验收

- 16 只股票最近至少一年日线成功率达到 95% 以上。
- 每张日线表通过主键唯一、日期递增、OHLC 合理性检查。
- 关键财务字段至少获取营业收入、净利润或归母净利润及报告期。
- 实时快照能获取动态市盈率、市净率、换手率等可用字段；缺失项需解释。
- 资金流、涨跌停池等受限接口必须记录实际覆盖范围。
- ETHUSDT 必须输出“成功、部分成功或 AKShare 不支持”的明确证据。

### 11.3 分析验收

- 计算全部指定均线。
- 对每只股票生成一年涨停/跌停次数和次日开盘统计。
- 生成活跃度评分及至少 5 个可解释子指标。
- 生成横盘/高波动/趋势等风格标签和置信度。
- 提供至少 3 条 SQL 查询及数据库表截图或结果表。

## 12. 最终交付物

1. `AKShare数据抓取测试_总纲.md`
2. `AKShare数据抓取测试_分阶段操作步骤.md`
3. 后续实施阶段的 Python 项目源码
4. `requirements.txt` 或 `pyproject.toml`
5. 原始数据和清洗数据样例
6. DuckDB/SQLite 数据库文件或 PostgreSQL 建表脚本
7. 数据字典与字段映射表
8. 数据质量检查结果
9. 测试数据展示图表
10. AKShare 数据搭建报告

## 13. 项目阶段概览

| 阶段 | 主要任务 | 阶段成果 |
|---|---|---|
| 0 | 需求冻结与口径定义 | 指标字典、测试范围 |
| 1 | 环境与项目骨架 | 可复现环境、目录结构 |
| 2 | 接口盘点与冒烟测试 | 接口能力矩阵、错误记录 |
| 3 | 行情与实时估值抓取 | 日线、实时快照、原始文件 |
| 4 | 财务与资金流抓取 | 财务表、指标、资金流 |
| 5 | 数据库与清洗 | 标准表、数据质量报告 |
| 6 | 技术与量价指标 | 均线、活跃度、波动指标 |
| 7 | 涨跌停事件研究 | 年度次数、次日表现 |
| 8 | 横盘及风格分析 | 风格标签、解释性证据 |
| 9 | ETHUSDT 能力验证 | 实时或历史数据结论 |
| 10 | 展示、复核与报告 | 数据库展示、图表、最终报告 |

## 14. 参考资料

- [AKShare GitHub 仓库](https://github.com/akfamily/akshare)
- [AKShare README](https://github.com/akfamily/akshare/blob/main/README.md)
- [AKShare 项目概览](https://akshare.akfamily.xyz/introduction.html)
- [AKShare 股票数据文档](https://akshare.akfamily.xyz/data/stock/stock.html)
- [AKShare 加密货币数据文档](https://akshare.akfamily.xyz/data/dc/dc.html)

